FILE LIST -
      uxBlastee.c

DESCRIPTION -
	     This is a server program which communicates with client through a
	     TCP socket. It is intended to be run on a UNIX system (hence the
	     name "ux" for UNIX). It allows to configure the maximum size of 
	     socket-level receive buffer. It repeatedly reads a given size 
	     message from the client and reports the number of bytes read every
	     minute.

RUNNING DEMO -
	     To run this blastee task from UNIX host do the following:
	     % blastee   7000  1000  16000 &

EXAMPLE COMPILE LINE -
	     cc -o uxBlastee uxBlastee.c -lsocket -lnsl
