FILE LIST -
             unixClient.c
             client.h

DESCRIPTION - 
	     Code to be executed by client. Writes request/message into socket
	     and optionally receives a reply back. Code executes on UNIX,
	     therefore UNIX header files are included.

RUNNING DEMO -
	     On the UNIX host:
	     % unixClient

TESTED ON -  Host/Target : Solaris 2.5 / mv162
	     VxWorks     : 5.3.1

EXAMPLE COMPILE LINE -
              cc -o uxClient uxClient.c -lsocket -lnsl

